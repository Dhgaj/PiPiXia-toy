# throw 语句测试

func checkAge(age: int) {
    if (age < 0) {
        throw "年龄不能为负数"
    }
    print("年龄有效")
}

func main(): int {
    try {
        checkAge(25)
    } catch (error: string) {
        print(error)
    }
    return 0
}
