# 目标文件生成测试
# 测试 compileToObjectFile() 功能

func add(a: int, b: int): int {
    return a + b
}

func main(): int {
    let x: int = 10
    let y: int = 20
    let result: int = add(x, y)
    print("${x} + ${y} = ${result} (应输出: 10 + 20 = 30)")
    return 0
}
